# using OOP + linkedlist, create a data structure that can show the list of pull request and help Boss Gi decide which pull request to process next
# add_request(name) method, which adds new request by name
# display_all() method, which shows all pull requests
# resolve() method, which shows who is next to be resolved
# any other methods you need

